# Copyright (c) Microsoft Corporation. All rights reserved.
# Licensed under the MIT License.
#
variables:
- group: AML-config
- name  : 'test'
  value : 'tests/ci/run_pytest.py'
- name  : 'testfolder'
  value : './tests/unit'
- name  : 'maxnodes'
  value : 4
- name  : 'reponame'
  value : 'nlp'

trigger: none

pr:
- staging 
- master

jobs:
- job: "NB_cpu_unit_test"
  timeoutInMinutes: 240

  variables:
  - name  : 'testmarkers'
    value : '"notebooks and not gpu and not azureml"'
  - name  : 'junitxml'
    value : 'reports/test-unit-nbcpu.xml'
  - name  : 'clustername'
  #underscores not allowed, only letters, digits and - char
    value : 'nlp-cpu'
  - name  : 'vmsize'
    value : 'STANDARD_D3_V2'
  - name  : 'dockerproc'
    value : 'cpu'
  - name  : 'expname'
    value : 'cpu_unit_tests'

  pool:
   vmImage: 'ubuntu-16.04'
  
  steps:
  
  - task: UsePythonVersion@0
    inputs:
      versionSpec: '3.6'
      architecture: 'x64'
    displayName: 'Use Python 3.6'
  
  - script: | 
     az login --service-principal -u $(spidentity) -p $(spsecret) --tenant $(sptenant)
    displayName: 'Login to Azure'
  
  - bash: |
      echo "##vso[task.prependpath]/data/anaconda/bin"
    displayName: Add Conda to PATH
  
  - script:
       pip install paramiko==2.4.2 &&
       pip install azureml-sdk &&
       pip install azure-cli
    displayName: 'install azureml-sdk'
  
  - script: 
       python tools/generate_conda_file.py --name nlp 
    displayName: ' generate_conda_file.py'
  
  - script: 
      python tests/ci/submit_azureml_pytest.py --subid $(SubscriptionID) --testfolder $(testfolder) --testmarkers $(testmarkers) --clustername $(clustername) --expname $(expname) --dockerproc $(dockerproc) --junitxml $(junitxml) --reponame $(reponame) --branch $(Build.SourceBranchName)
    displayName: 'submit_azureml_pytest'
  
  - task: PublishTestResults@2
    displayName: 'Publish Test Results **/test-*.xml'
    inputs:
      testResultsFiles: '**/test-*.xml'
      failTaskOnFailedTests: true
      testRunTitle: 'AzureML notebooks cpu unit tests'
    condition: succeededOrFailed()

- job: "NB_gpu_unit_test"
  timeoutInMinutes: 240

  variables:
  - name  : 'testmarkers'
    value : '"not notebooks and gpu and not azureml"'
  - name  : 'junitxml'
    value : 'reports/test-unit-nbgpu.xml'
  - name  : 'clustername'
  #underscores not allowed, only letters, digits and - char
    value : 'nlp-gpu'
  - name  : 'vmsize'
    value : 'STANDARD_NC6'
  - name  : 'dockerproc'
    value : 'gpu'
  - name  : 'expname'
    value : 'gpu_unit_tests'

  pool:
    vmImage: 'ubuntu-16.04'
  
  steps:
  
  - task: UsePythonVersion@0
    inputs:
      versionSpec: '3.6'
      architecture: 'x64'
    displayName: 'Use Python 3.6'
  
  - script: | 
     az login --service-principal -u $(spidentity) -p $(spsecret) --tenant $(sptenant)
    displayName: 'Login to Azure'
  
  - bash: |
      echo "##vso[task.prependpath]/data/anaconda/bin"
    displayName: Add Conda to PATH
  
  - script:
       pip install paramiko==2.4.2 &&
       pip install azureml-sdk &&
       pip install azure-cli
    displayName: 'install azureml-sdk'
  
  - script: 
       python tools/generate_conda_file.py --name nlp 
    displayName: ' generate_conda_file.py'
  
  - script: 
      python tests/ci/submit_azureml_pytest.py --subid $(SubscriptionID) --testfolder $(testfolder) --testmarkers $(testmarkers) --clustername $(clustername) --expname $(expname) --dockerproc $(dockerproc) --junitxml $(junitxml) --reponame $(reponame) --branch $(Build.SourceBranchName)
    displayName: 'submit_azureml_pytest'
  
  - task: PublishTestResults@2
    displayName: 'Publish Test Results **/test-*.xml'
    inputs:
      testResultsFiles: '**/test-*.xml'
      failTaskOnFailedTests: true
      testRunTitle: 'AzureML notebooks gpu unit tests'
    condition: succeededOrFailed()
